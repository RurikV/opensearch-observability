# OpenSearch 3.x requires a custom admin password (the demo admin/admin was removed
# in 2.12). Must be >= 8 chars with upper+lower+digit+special.
OPENSEARCH_ADMIN_PASSWORD=Z8#vQ2!mR7@kP4nX

# Image versions (verified current as of 2026-07).
OPENSEARCH_VERSION=3.7.0
FLUENT_BIT_VERSION=5.0.9

# Absolute host path to the SEPARATELY-DEPLOYED WooCommerce store's logs directory.
# The store lives in ../woocommerce-observability; Fluent Bit mounts these read-only.
WC_LOGS_DIR=/Users/rurik/repos/woocommerce-observability/logs

# least-privilege user for write-path integrations (create via security/setup-observability-user.sh)
OBS_PASSWORD=K7#pQ2mR9xL4wT
